name: ej_selector
description: A simple Item selector widget which uses dialog to select items.
version: 0.0.3+1
homepage: https://github.com/EhsnJmli/ej_selector

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: